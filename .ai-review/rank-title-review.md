# 結算稱號 — 人工法規覆核紀錄

> 對應 `.ai-review/plan-v4-engagement.md` 的 **D24 條 4** 與驗收條件 **A29**
> 表格來源：`engine.js` 的 `RANK_TITLES`（唯一真相）
> 建立日期：2026-08-06
> **狀態：待人工定案**（自動化黑名單已通過；用字本身仍是暫定）

---

## 1. 覆核什麼

D24 條 4 要求：稱號**不得使用台灣有法定或認證意義的職稱**，且用字須做一次人工覆核並留檔。
自動化測試（`tests/engagement.test.mjs` 的 A29）擋的是**封閉黑名單的字面命中**，
擋不到的是「整體語氣讀起來像在授予資格」這種需要人判斷的部分——那是本文件存在的理由。

黑名單完整列於 A29 測試檔中，不以「等」結尾（開放清單無法測）。

## 2. 逐格覆核

| 級別 | 分數帶 | 稱號 | 法定職稱命中 | 人工判定 |
|---|---|---|---|---|
| L1 簡單 | ≥95 | 牌面熟手 | 無 | 待確認 |
| L1 簡單 | ≥80 | 記得住外觀 | 無 | 待確認 |
| L1 簡單 | ≥50 | 還在對圖 | 無 | 待確認 |
| L1 簡單 | <50 | 先從閃卡開始 | 無 | 待確認 |
| L2 中級 | ≥95 | 對得很快 | 無 | 待確認 |
| L2 中級 | ≥80 | 看得出門道 | 無 | 待確認 |
| L2 中級 | ≥50 | 還在比對 | 無 | 待確認 |
| L2 中級 | <50 | 先從閃卡開始 | 無 | 待確認 |
| L3 困難 | ≥95 | 一眼認藥 | 無 | 待確認 |
| L3 困難 | ≥80 | 火眼金睛 | 無 | 待確認 |
| L3 困難 | ≥50 | 還在翻仕單 | 無 | 待確認 |
| L3 困難 | <50 | 先從閃卡開始 | 無 | 待確認 |

## 3. 覆核時已知的三個疑點（需使用者裁決）

### 3.1 L3 的 ≥95 與 ≥80 讀起來像反的

`≥95 一眼認藥` / `≥80 火眼金睛`。「火眼金睛」在一般語感上強於「一眼認藥」，
同一級別內使用者會直接感受到這個高低關係。D24 條 1 雖然明文拒絕**跨級**序數模型，
但**同級內**的分數帶本來就是有序的，稱號讀起來與分數方向相反會被當成 bug。

建議：兩格對調，或把 ≥80 改成語氣較弱的詞。**未逕自更動**——表就是唯一真相，
改表要走同一道人工覆核。

### 3.2 「還在翻仕單」的用字

「仕單」是院內口語，非正式書面用語，且非台中以外的使用者未必看得懂。
若確認要保留，本文件即為留檔依據；若要改，建議「還在查品項」。

### 3.3 「先從閃卡開始」三級共用

這一列刻意三級相同（它是下一步建議而非稱號，最低帶也沒有跨級刷分誘因）。
A28 有一條**正面斷言**釘住這件事，避免日後被當成漏寫而各自改掉。

## 4. 定案後要做的事

1. 修改 `engine.js` 的 `RANK_TITLES`
2. 同步修改 `tests/engagement.test.mjs` 的 `TITLES`（該表刻意寫死，不從實作反推）
3. 更新本文件的「人工判定」欄與狀態
4. 升 `sw.js` 的 CACHE 版本並同步 `tests/sw.test.mjs` 的 C31
