# v3 難度分級 — 階段性交接

> 2026-08-01 收尾。分支 `feat/difficulty-levels`，**未 push、未合併 main**。

## 狀態

三級全部實作完成，**119 項測試全綠、`npm run verify` 全綠**。

| 級別 | 題型 | 狀態 |
|---|---|---|
| L1 簡單 | 看圖 → 四個藥名擇一 | ✅ |
| L2 中級 | 給藥名 → 四張圖擇一 | ✅ |
| L3 困難 | 看圖 → 輸入英文品名（原有） | ✅ 未回歸 |

```
e39146d feat(l2): 中級（給藥名，四張圖擇一）
8ca1b57 feat(ui): 簡單級 L1（看圖四選一藥名）
151539e feat(engine): 難度分級基礎建設（兩階段生成、身分鏈、逐題基線）
3db2fea docs(review): v3 難度分級規格、Codex 覆審與 G1 gate
```

## 未完成的事

**1. ~~沒有做過真實瀏覽器測試~~ — 2026-08-01 已補做。** 見下方「真機測試結果」。

**2. C14／C15 已在 localhost 量測（見下），但**尚未在正式 GH Pages（含壓縮）複驗。
localhost 無 gzip，量到的是最壞值，兩項都仍通過門檻，因此 GH Pages 只會更寬鬆。

**3. 尚未 push、未開 PR、未合併。**

## 真機測試結果（2026-08-01，Chrome，375×812）

視窗有 Chrome 最小寬度限制（實測 innerWidth 627），拿不到真正 375px 視埠。
改以**同源 iframe 固定 375×812**——media query 依 iframe 視埠求值，
`matchMedia('(max-width: 480px)')` 為 true，等同真實 375px 渲染。

| 項目 | 結果 |
|---|---|
| C17 縮放未停用 | ✅ viewport meta 為 `width=device-width, initial-scale=1.0` |
| C12 無橫向捲動 | ✅ `documentElement.scrollWidth` 356 ≤ 375；三級的答題頁與成績頁溢出元素皆為 **0** |
| C12 四格落在視埠內 | ✅ 四格各 141.1×141.1px（含 19px 捲軸；真機無捲軸約 148px） |
| C12 選項觸控目標 | ✅ 141px，遠高於 44px；四格中心 hit-test 皆命中自身，無遮擋 |
| C15 執行期圖片流量 | ✅ 20 題 **80 個請求**（＝20×4，零替換）、transfer **0.94MB** < 3MB；實測均 11.7KB/張（優於 F6 的 13.0KB） |
| C14 非題目圖片資源 | ✅ **2.16MB** < 3MB（localhost 未壓縮的最壞值）。組成：`pool.json` 2154KB、Google Fonts `css2` 354KB、`app.js` 31KB、`engine.js` 24KB |
| C13 成績卡實際版面 | ✅ 1440×1800 PNG：級別 badge、分數、亂猜基線、淨值、逐題雙欄清單、頁尾免責皆完整，無裁切、無畫布外繪製 |
| 三級 e2e 回歸 | ✅ L1／L2／L3 各跑完 20 題整回合，成績頁與逐題加總基線正確（L1 25.0／L2 25.0／L3 0.0） |

**真機抓到、樁測不到的兩個瑕疵（已修）**：

- **`app.js:612` 顯示「待加強（中級級標準）」** ——`LEVELS.L2.name` 是「中級」（已含「級」），
  該行卻寫 `${cfg.name}級標準`。L1／L3 的 name 是「簡單」「困難」所以看不出來。
  改用 `cfg.badge`（其餘 4 處顯示路徑本來就用 badge，這行是唯一的例外）。
- **所有動作按鈕高 40.1px < 44px 觸控下限。** 基礎 `button` 規則沒有 `min-height`，
  只有 `#qInput` 有。四宮格因為是圖片格不受影響，所以 C12 的字面條件（「選項」）
  其實有通過——但提示／送出／下一題／下載成績卡全都不足。
  於 `index.html` 基礎 `button` 補 `min-height: 44px`，與 `#qInput` 同一標準。

修正後 119 項測試仍全綠。

**仍未驗**：pinch-zoom 在四宮格內的實際手感（需真手指，自動化測不到）。

本機驗證用：專案根目錄起一個靜態 server 即可（純靜態、零建置）。

## ✅ 合併阻斷項（2026-08-01 `/codex-review` 結果 → 2026-08-03 已修）

覆審已跑完。原始報告 `.ai-review/codex-review-v3.md`，逐項判定
`.ai-review/verdict-v3.md`（**接受 16／部分接受 3／拒絕 0**）。

唯一的實作缺陷（#1 遞補回溯破壞 H2 + 抽樣不均、#4 缺整卷 validator）**已於 2026-08-03 修復**，
見規格 v3.5 與下方待修清單。測試 119 → **136 全綠**，`npm run verify` 全綠。

其餘 18 項是測試強度與文件落差，可在合併後以獨立 commit 補強，優先順序見 `verdict-v3.md`。

**兩項不要誤處理**：

- **G1 gate**（Codex 評 High）是**已知且已決議**的流程項，不是實作瞞混。維持原決議。
- **`update-pool.yml` Actions 未固定 SHA**（Codex 評 High，判定正確）是 `main` 既有問題，
  **另案**處理，不阻斷本次合併。

## 待修清單（2026-08-03 更新）

理由與判定依據見 `verdict-v3.md` 的對應編號，此處只列「要做什麼」。

**擋合併**

- [x] **#1+#4 `drawSpare()` 遞補 + 整卷 validator** —— 2026-08-03 完成。
      遞補移入 engine 的 `drawSpareQuestion()`：屏障由「正解集合」擴為
      `seenAnsKeys()`（正解 ∪ 選項 ∪ 備援），並在答案鍵層級均勻抽樣；
      新增 `validateQuizInvariants()`，生成路徑違規拋 `INVARIANT_VIOLATED`（不重抽），
      遞補路徑違規中止本回合。新增驗收 A21／A22（+17 測試）。
      兩個缺陷各以 **mutation 驗證測試有鑑別力**，且必須分開驗——
      決定性抽樣會讓遞補樣本恆為 1，反而掩蓋屏障缺陷（合起來只有 1 條會紅）。

**合併後補，依優先序**

- [ ] #10 C17 測試斷言反向（`tests/ui-smoke.test.mjs:187`）——真 bug，一行
- [ ] #11 C8 拿掉 `length >= 2` 過濾、納入 `title`／`aria-label`（`:231`）
- [ ] #13 C11 補「同題同格已換 src」反例——真實瀏覽器最常見路徑，測試反而沒蓋
- [ ] #3 L1 eligibility 補誘答兩兩 H3 判定（`engine.js:490-495`）
- [ ] #7 A10 空 `shape`／`color` guard（`engine.js:341` `disjoint([],set)` 恆真）
      ＋ `verify` 補非空斷言
- [ ] #12／#15／#16 補 C9/C10 失敗矩陣、C16 三級答對與提示、A18 的 L2 全鏈路
- [ ] #14 C13 改用真 Canvas 產 PNG（真機版面已人工驗過，缺的是自動化）
- [ ] #5 規格回填：把 `slotAsset[]` 改寫為「現值讀取」（**不要**加平行陣列）
- [ ] #6 `img.decode()` 以貼合 D21 字面
- [ ] #9 §4.4 紅線 3,000 → 2,850；#17 A20 補邊界；#18 `app.js:113,266+` 補 escape
- [ ] #8 正式 GH Pages 冷快取 trace 複驗 C14

**另案（非本分支造成）**

- [ ] #19 `update-pool.yml` 的 Actions 固定完整 SHA、鎖 Pillow/requests 版本
      ——`main` 既有的供應鏈入口風險，下次自動題庫更新前處理

**不處理**

- G1 gate 補跑 30 題 —— 已決議目視結案，僅在出現「題目不可解」回報時才跑（seed `20260801`）

## 合併前建議

跑一次 `/codex-review` 覆審這三個 commit。理由不是形式：
v3 規格的驗收條件是用「什麼樣的弱化實作能讓這條通過但功能其實是壞的」
反推出來的，而**規格審查通過不代表實作遵守規格**——那道縫正是
`/codex-review` 區塊 4（規格符合度稽核）要補的。比對基準是
`.ai-review/plan-v3-levels.md` 的 A8–A20、C7–C17。

## 實作過程中改過的規格（都已回填 plan-v3-levels.md）

| 版本 | 改了什麼 | 為什麼 |
|---|---|---|
| v3.1 | 依 Codex 覆審修訂 47 項 | 接受 42／部分接受 5／拒絕 3（範圍蔓延 0） |
| v3.2 | 移除 G1 的「不可放大」條件；新增 D22／C17 | pinch-zoom 是原生能力，要求不放大即可辨識是憑空多出的限制。代價是縮放變成 L2 可解性的前提，必須有測試守住 |
| v3.3 | `INSUFFICIENT_KEYS` 門檻 `n` → `n + 誘答數`；L1 誘答比對改用外觀聯集 | 門檻寫 `n` 時全部答案鍵都是正解、合法誘答為 0，必然組不出題（測試抓出） |
| v3.4 | 新增 F7；A9 門檻 ≥3000 → ≥2850 | F2 的 3009 量的是較寬的條件，不能當實作基準 |

## 三個容易再踩的坑

**L1 與 L2 的誘答條件本質不同。** L1 顯示藥名 → 比對該藥名**所有紀錄**的外觀聯集；
L2 顯示特定照片 → 只比對**那筆紀錄**。用錯層級會讓其中一級的可解性保證失效。

**L2 的 `alt` 只能是中性描述。** 沿用 L1/L3 的
`藥品外觀實拍圖：${shape}／${color}` 會把答案寫進可及性樹——
畫面上看不到，但該級直接歸零。C8 斷言的是可及性樹，不是可見文字。

**圖片事件的生效條件是三段身分全等**：題目 token、格位、該格目前的資產 id。
只比對題號會被遞補的位移騙過；只比對格位會被「同格換過備援」的舊請求騙過
（那是真實瀏覽器最常見的路徑）。

## G1 gate 的證據等級

**目視判定通過，未跑完整 30 題計分**，所以「≥80%」這個門檻**實際上沒有被量測**。
若 L2 上線後出現「題目不可解」的回報，不可視為「gate 量錯了」，
而應視為 **gate 未曾量化**——屆時跑完整 30 題，而不是調整門檻。
樣本產生器 `.ai-review/g1-sample.mjs` 已留檔（seed `20260801`，可重現）。
