# 覆審逐項判定 — v3 難度分級

> **日期**：2026-08-01
> **對象**：`.ai-review/codex-review-v3.md`（codex-cli 0.146.0，read-only）
> **範圍**：`feat/difficulty-levels` 相對 `main`（`151539e`／`8ca1b57`／`e39146d`／`8806bef`）
> **覆核方式**：每項均回讀該檔該行驗證，不採信 Codex 描述。

## 統計

**接受 16／部分接受 3／拒絕 0**

本輪 Codex **無幻覺、無架構誤解**——未出現「拆模組」「加 Service Worker」「放寬容錯」
這類與已聲明限制衝突的建議，XSS 自評 Low，供應鏈項自行註明屬 `main` 既有問題。
前提段落有被實際吃進去。

## 判定表

| # | 項目 | 嚴重度 | 判定 | 理由 |
|---|------|--------|------|------|
| 1 | `drawSpare()` 遞補破壞 H2 且抽樣不均 | High | **接受** | 驗證屬實。`app.js:204,459` 的 `state.used` 只裝**正解**答案鍵；`app.js:474` 僅以此過濾，因此遞補題的正解可以是「早先某題誘答」的答案鍵，回溯破壞 H2——正是 D19 要防的失效。另 `app.js:473` 按 pool 順序取第一筆合格者，決定性而非均勻抽樣，違反 v2 D5。修法可行。 |
| 2 | G1 gate 從未量化 | High | **接受** | 屬實。規格 `plan-v3-levels.md:40` 要求 ≥24/30，`HANDOFF-v3.md` 自承目視結案、無正確率數字。**但這是使用者已知並刻意做出的判斷**（見 memory `tfda_drug_id_quiz`），非實作瞞混。處置照原決議：若出現「題目不可解」回報，跑完整 30 題（seed `20260801` 可重現），**不調門檻**。 |
| 3 | L1 eligibility 未驗誘答彼此 H3 | Medium | **接受** | 驗證屬實。`engine.js:490-495` 只逐一累計 `candidateOk()` 通過數，達 `need` 即回 true，未檢查候選**兩兩** H3；而階段 2 的 `pickMutuallyValid()` 有檢查。故 `eligibleKeys()` 可能判可用、階段 2 才失敗，與 D18「E 的每筆紀錄都能組題」定義不符。 |
| 4 | 缺整卷不變量 validator | Medium | **接受** | 驗證屬實。規格 D19 明列回傳前執行 H1–H4／I1–I5 驗證；`engine.js:591` 直接 `corrects.map()` 後回傳，無執行期檢查。測試通過不等於規格要求的最後防線存在。與第 1 項同源，應一併修。 |
| 5 | `slotAsset[]` 宣稱存在但從未產生 | Medium | **部分接受** | 欄位確實不存在（全 repo 零出現；規格 `plan-v3-levels.md:550,553` 有宣告）。**但它要保護的不變量已被實際執行**——`app.js:360,389` 以 `cur.options[k]?.id` 現值比對，而 `replaceOption()`（`engine.js:638-640`）是原子換陣列，讀到的必然是該格當下資產。**正確修法是改規格而非加欄位**：平行陣列多一份可失步的狀態，比現行單一真相源更差。降為 Low。 |
| 6 | D21 未等待 `img.decode()` | Medium | **部分接受** | 屬實（`app.js:380` 只用 `onload` + `naturalWidth`）。但實務落差窄：`<img>` 的 `onload` 在主流瀏覽器本就在解碼成功後才觸發，解碼失敗走 `onerror`，且零尺寸已被攔。值得補以貼合規格字面，非合併阻斷項。降為 Low。 |
| 7 | A10 空集合可成為合法 L1 誘答 | Medium | **部分接受** | 程式面屬實：`engine.js:341` `disjoint([], set)` 恆為 `true`。**但現行題庫 3,913 筆的 `shape`／`color` 皆非空（已實測）**，目前不可達。考量本專案資料源 schema 會變（見 `CLAUDE.md`），且空外觀誘答＝與正解無法區分＝命中「題目不可解」風險，仍建議加 guard 並在 `verify` 補斷言。降為 Low。 |
| 8 | C14 未在正式 GH Pages 驗收 | Medium | **接受** | 屬實，且與我自己在 `HANDOFF-v3.md` 的記載一致。localhost 未壓縮量得 2.16MB < 3MB 是有利證據但非規格要求的證據型別。 |
| 9 | §4.4 紅線 3,000 與 A9 的 2,850 不一致 | Low | **接受** | 屬實，v3.4 只改了 A9 沒回填 §4.4。純文件修正。 |
| 10 | C17 測試斷言反了 | Low | **接受** | 驗證屬實且是真的測試 bug：`tests/ui-smoke.test.mjs:187` 用 `doesNotMatch(/touch-action:\s*(none\|pinch-zoom)/)`，把**合法且正是 D22 所欲**的 `touch-action: pinch-zoom` 判為失敗；`maximum-scale=[1-4]` 亦漏掉 `0.5`。現在會綠是因為 code 兩者都沒寫。 |
| 11 | C8 測試以長度 <2 過濾特徵值 | Medium | **接受** | 驗證屬實：`tests/ui-smoke.test.mjs:231` 的 `String(v).length >= 2` 會讓「白」「紅」「圓」等單字特徵即使洩漏也假綠；`surface` 也未納入 `title`／`aria-label`。**產品碼本身正確**（真機實測 alt 為「選項 A 藥品外觀」），純屬測試強度不足。 |
| 12 | C9／C10 失敗矩陣不全 | Medium | **接受** | 屬實：未測兩格同時失敗、替換圖再失敗、備援耗盡、重送同一錯誤事件。 |
| 13 | C11 缺「同題同格已換 src」反例 | Medium | **接受** | 屬實，且這正是 `HANDOFF-v3.md` 自己標為「真實瀏覽器最常見路徑」的那條，測試反而沒蓋到。 |
| 14 | C13 用 `{size:1}` 樁取代真 blob | Medium | **接受** | 屬實。**惟本次真機測試已補上實證**：實際產出 1440×1800 PNG、版面完整無裁切（見 `HANDOFF-v3.md`）。自動化仍應補。 |
| 15 | C16 三級 e2e 未含答對／提示 | Medium | **接受** | 屬實：L3 全程無答對、無提示、無重複送出；L2 未用提示。 |
| 16 | A18 身分鏈只驗 L1 engine 快照 | Medium | **接受** | 屬實，缺 L2 從 engine → DOM → 判定 → 檢討的全鏈路案例。 |
| 17 | A20 邊界只測 L1 22 鍵 | Low | **接受** | 屬實。實作門檻本身正確（L1 23／L2 25／L3 20），缺的是邊界測試。 |
| 18 | 題庫文字未 escape 即進 `innerHTML` | Low | **接受** | 屬實：`app.js:113` 的 `${m.source}`、`app.js:266+` 的 `featureCell()` 未 escape，而 `escapeHtml()` 已存在且在其餘 8 處一致使用——屬遺漏而非設計。依威脅模型（同源、無憑證、資料出自自家管線）Low 正確。 |
| 19 | `update-pool.yml` Actions 未固定 SHA | High | **接受（不在本次範圍）** | 屬實且評級正確——污染題庫的入口對本專案是 High。但 Codex 自己已註明此為 `main` 既有問題，非這 4 個 commit 造成。**應另開工作項，不阻斷本次合併決策**。 |

## 必修項（Critical/High 且判定為接受）

1. **#1 `drawSpare()` 遞補**（High）——連同 **#4 整卷 validator** 一起修。
   遞補須改為：從「未使用**且未出現在任何現存 options/spares**」的可用答案鍵中均勻抽正解 →
   該鍵紀錄中均勻抽一筆 → 組選項 → **重跑整卷不變量驗證**；無候選則明確中止。
   這一項是唯一真正的功能缺陷，其餘皆為測試強度或文件落差。

2. **#2 G1 gate**（High）——**非程式問題**。使用者已於 2026-08-01 以目視判定結案並知悉
   門檻未量測。維持原決議即可，不因外部覆審而改判；若要關閉此項爭議，
   就跑完整 30 題計分（seed `20260801`）。

## 建議處理順序

1. #1 + #4（同一處修法，一起做）
2. #10 C17 測試斷言反向（真 bug，一行）
3. #11 C8 長度過濾 + `title`／`aria-label`（測試強度，守的是 L2 最高風險）
4. #13 C11 同格換資產反例
5. #3 L1 eligibility H3、#7 A10 guard、#5/#6 規格回填或補 decode
6. #9 §4.4 文件、#17 A20 邊界、#18 escape 遺漏
7. #8 C14 正式 Pages trace（需部署後）
8. #19 workflow SHA 固定——**另案**，下次自動題庫更新前處理

## 合併判定

**同意 Codex 的「不可合併」結論，但理由收斂為一項**：#1 遞補邏輯。

Codex 列的 6 條必修中，只有 #1（含 #4）是實作缺陷；#2 是已知並已決議的流程項，
其餘為測試強度與文件落差，可在合併後以獨立 commit 補強。
若要最小阻斷，**修完 #1 + #4 即可合併**，其餘轉為 follow-up。
