# v3.5 覆審逐項判定

> 對象：`.ai-review/codex-review-v3.5.md`（commit `f0b7038`）
> 判定日期：2026-08-03。判定者已逐項回讀該檔該行驗證，不採信描述。

**統計：接受 5／部分接受 5／拒絕 0。無 Critical／High。**

Codex 的核心結論成立且與我一致：**前輪 #1（遞補回溯破壞 H2）已實質修復**；
**前輪 #4 只做了一半**——`validateQuizInvariants()` 沒有、也不可能達成 D19 字面上
「驗證 H1–H4、I1–I5」的宣稱。缺陷不在程式行為，而在**宣稱過強**。

## 判定表

| # | 項目 | 嚴重度 | 判定 | 理由 |
|---|------|--------|------|------|
| 1.1 | validator 未涵蓋完整 I1–I5 | Medium | **部分接受** | 逐項驗證：**I2 由 I1 蘊含**（`engine.js:674` 比對物件同一性 `opts[q.answerIdx] !== q.item`，題幹讀 `q.item`、正解格讀 `options[answerIdx]`，同一性成立即 I2 成立）——屬實，但只需註明。**I4 屬實且無解**：它是跨渲染的快照引用規則，靜態 `Question[]` 沒有渲染歷程可驗，守它的是 C11／A18。**I5 屬實**：`engine.js:648-650` 只驗題目 token 唯一，規格的三元組還有格位與資產 identity。**I3 屬實且可以真正修**：`engine.js:686-691` 只驗 `id` 非空＋同題不重複，捏造唯一 id 可通過。**但正確修法比 Codex 建議的更強也更便宜**——傳入 `PoolIndex` 後直接驗 `index.byAns.get(r.ans)?.includes(r)` 的**物件同一性**，不必逐欄比對「id 存在且欄位一致」。**拒絕**「另建 UI validator 驗 I4/I5」：C11／A18 已是那條路徑的驗收，新建元件是範圍蔓延。 |
| 1.2 | H4 檢查是較弱現象，非 H4 本身 | Medium | **接受（降 Low）** | 驗證屬實，`engine.js:705-708` 確為「整卷 `answerIdx` 全同」的 heuristic。偽陰性清單正確（循環／依題號決定皆可通過）。`>= 10` 確無規格依據，是我隨手訂的——改為 `QUIZ_SIZE`，偽陽性由 3.8×10⁻⁶ 降至 3.6×10⁻¹²。**保留這項檢查**：成本為零，抓的是「忘記洗牌」這種災難級且畫面上看不出來的失效。降為 Low 的理由是**程式行為本身沒有錯**，錯的是把它叫做 H4——這是命名與文件問題。H4 的真正證據是 A13(a)–(d)（`tests/levels.test.mjs:278,288,299,313`，含「題號不能決定位置」與 500 題分布），Codex 未查到這點。 |
| 1.3 | 遞補抽樣非嚴格均勻 | Low | **部分接受** | 理論屬實：`shuffle` 後取第一個能組出誘答的鍵，若各鍵失敗率不同就會偏向易組題者。**但 Codex 的量測情境不對，實測偏差為 0**——在真正的遞補脈絡（`excludeAns` ＝整卷 20 個正解）下，L1 候選鍵 3,044 個、L2 2,939 個，**組不出誘答者皆為 0**；4,000 次遞補的相異鍵 2,195／2,188、最高頻 7／8（期望 1.31／1.36，符合 Poisson 上尾）。Codex 的「L2 9/10929 失敗」量的是別的條件。**拒絕修法**：先算可行集合再抽樣＝把 1.6 秒的 eligibility 掃描搬進遞補路徑，違反規格已聲明的「不快取會卡住主執行緒」，換來 0 的實測收益。**接受文字精確化**並把量測寫進註解——未來題庫變動使失敗率上升時，這段量測就是重新評估的基準。 |
| 1.4 | fatal 訊息顯示答案鍵 | Low | **接受** | 屬實。`app.js:196,477` 直接把違規訊息第一筆給使用者，而 `engine.js:652,681,694-696` 的訊息含 `ans`。已 escape 故非 XSS，但會在中止畫面揭露未作答題目的藥名。改為畫面只給違規代碼、細節走 `console.error`。 |
| 2.1 | A21 均勻性測試只能排除決定性實作 | Low | **接受** | 屬實。60 seed／≥20 相異值擋得掉「取第一筆」，擋不掉高度偏斜。改為連續 RNG 大樣本 + **最高頻上限**（配合 1.3 的量測，這是有基準的斷言）。 |
| 2.2 | A22 mutation 缺 I2／I4／完整 I5／捏造 id | Medium | **部分接受** | **捏造 id 案例接受**（配合 1.1 的 I3 強化，屆時才測得出來）。**I2 拒絕**：由 I1 蘊含，另立測試是同一件事驗兩次，只會在未來製造漂移。**I4 拒絕**：不在 validator 職責內，正確處置是規格分層寫明，不是加測試。**H4 那條部分接受**：對 A22 而言屬實，但「固定循環／依題號決定」已由 **A13(c)** 覆蓋（`tests/levels.test.mjs:299`），Codex 漏查；要補的是規格把 H4 的證據明確指向 A13。降 Low。 |
| 2.3 | UI C10 未跟上新遞補與 validate-then-pop | Medium | **部分接受** | 屬實（`tests/ui-smoke.test.mjs:290-300` 確實只驗自動前進／作廢 1 題／計分 20 題）。**但多數項目與既有待修 #12（C9/C10 失敗矩陣）重疊，本來就排在合併後。** 唯一應**提前**的是「遞補正解未出現在原卷任何 options/spares」——那正是本次修的缺陷，缺 UI 層斷言等於 app 傳錯 `questions` 也不會紅。validator-failure 分支需注入壞狀態才測得到，併入 #12。 |
| 3 | 依賴稽核 | — | **接受** | 屬實，`package.json` 無 dependencies／devDependencies，本 commit 未新增。 |
| 4.1 | D19「全不變量驗證」宣稱不成立 | Medium | **接受** | 屬實且是本輪最重要的一項。規格 `plan-v3-levels.md:340` 寫「最後對整卷做一次全不變量驗證（H1–H4、I1–I5）」，實作做不到也不該做到。修法：規格把不變量**分層**為「純題卷可驗」（H1–H3、I1、I3）、「heuristic」（H4）、「UI／事件鏈，由 C11／A18 驗」（I4、I5 的格位與資產部分）。 |
| 4.2 | §5 的 `usedAns`／`slotAsset[]` 從未產生 | Low | **接受** | 屬實。`slotAsset[]` 與前輪 #5 的判定一致（改規格而非加平行陣列，理由：平行陣列多一份可失步的狀態）。`usedAns` 是本次 v3.5 刪 `state.used` 後新產生的落差——同樣改為衍生值（`seenAnsKeys(questions)`），並寫明來源。 |
| 4.3 | A21／v3.5 的「均勻抽」宣稱過強 | Low | **接受** | 同 1.3，改文字並附實測基準。 |
| 4.4 | C10 自動驗收宣稱未完整落實 | Medium | **部分接受** | 同 2.3。 |

## 必修項（Critical/High 且接受）

**無。** 本輪未出現 Critical／High，也未推翻 v3.5 的功能修正。

## 建議處理順序

**合併前（本輪新增，都很小）**

1. **4.1 + 1.1 + 1.2**：規格把不變量分層；validator 收 `index` 參數把 I3 升級為物件同一性檢查；
   H4 那段改名為 heuristic、門檻 `10 → QUIZ_SIZE`；JSDoc 寫明 I2 由 I1 蘊含、I4 不在職責內。
2. **2.2（捏造 id）+ 2.1**：A22 補 fabricated-record mutation；A21 改大樣本 + 最高頻上限。
3. **1.4**：fatal 訊息改代碼制，細節走 console。
4. **2.3 的提前項**：C10 補「遞補正解不在原卷 options/spares」一條 UI 斷言。
5. **4.2 + 4.3**：規格 §5 移除 `usedAns`／`slotAsset[]` 並寫明衍生來源；A21／v3.5 文字精確化。

**合併後**（併入既有待修清單）

- 2.3／4.4 其餘項目 → 併入 #12（C9/C10 失敗矩陣），一併補 validator-failure 分支注入測試。

## 未採信之處（供稽核）

- Codex 1.3 的「L2 9/10929 組題失敗」在遞補脈絡下**複驗為 0**（量測腳本邏輯見判定表 1.3）。
  結論方向仍採納（文字精確化），但**不採納其嚴重度依據**。
- Codex 2.2 稱 H4 弱實作「仍可通過」——就 A22 而言屬實，但它漏查了 A13(c)／(d)。
