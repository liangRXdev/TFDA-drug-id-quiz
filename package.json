{
  "name": "tfda-drug-id-quiz",
  "version": "1.0.0",
  "description": "藥品辨識王 — 藥師外觀辨識自我測驗（純靜態，零建置）",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "build:pool": "node tools/build-pool.mjs",
    "fetch:images": "uv run --locked --script tools/fetch-images.py",
    "verify": "node tools/verify-data.mjs",
    "icons": "uv run --script tools/make-icons.py"
  },
  "engines": {
    "node": ">=22"
  }
}
